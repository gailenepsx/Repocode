The business need is to adapt the simulation process to support a monthly alert distribution instead of the current daily distribution. � This change does not aim to modify the detection logic, model behavior, or alert quality, and no degradation is expected in hit detection. � The expected impact is limited to the distribution level, where daily hits detected over the course of a month must be grouped and distributed as a single monthly alert. � The objective is therefore to align the simulator output with the target monthly distribution approach while preserving the existing detection capability and model logic.
