The simulator shall support the transition from daily to monthly alert distribution for the impacted models FTF, TSD, and EFT by grouping daily hits belonging to the same month into a single monthly alert, while preserving the existing detection logic and avoiding any degradation in detection results. � The current ALERT_ID field shall be reused as the structural grouping key for the monthly alert, as it defines how transactions are merged and therefore drives the alert population submitted to scoring and global consolidation. � A new month field and a retained daily_alert_id field shall be added to the impacted output files to ensure traceability between the monthly alert view and the underlying daily occurrences. � For countries not yet in scope for the monthly rollout, the simulator shall preserve the current daily alert flow through a country-level configuration in the JSON parameter file.
